{
    "providers": ["docker"],
    "build": {
        "dockerfilePath": "./Dockerfile"
    },
    "start": {
        "cmd": "php -m | grep -E 'PDO|pdo_mysql|mysqli' || true; php -S 0.0.0.0:$PORT -c php.ini -t ."
    }
}